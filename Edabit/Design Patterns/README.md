# 🧩 Design Patterns I: JavaScript Classes?!?

## Description
JavaScript doesn't really have classes like other languages — they are actually functions behind the scenes.  
There are several ways to create classes.

---

## 🎯 Challenge

### 1. **Book Class**
Create a **Book** class using a **JavaScript function** — *instantiable*.

It should have:
- `author` property  
- `published` property

---

### 2. **Author Class**
Create an **Author** class using a **literal object** — *singleton*.

It should have:
- `name` property  
- `books` property

---

### 3. **Publisher Class**
Create a **Publisher** class using the **new constructor** and an **anonymous function** — *singleton*.

It should have:
- `authors` property  
- `books` property

---

### 4. **Review Class**
Create a **Review** class using a **class declaration** — *instantiable*.

It should have:
- `rating` property  
- `user` property

---

## 💎 Bonus (optional)
Create a **Bookstore** class using an **IIFE (Immediately Invoked Function Expression)** — *singleton*.

It should have:
- `books` property  
- `prices` property

---

## 🧠 Notes
- All class names should begin with **Capital letters**.  
- All class properties should have **default (initial) values**.  
- There are a few other ways to make classes, but they are not part of this challenge.  
  You can check the **Resources** tab to learn more.
